services:
  web:
    build: .
    image: identity_thief_web:latest
    ports:
      - "5002:5000"
    environment:
      - PORT=5000
    volumes:
      - .:/app
    working_dir: /app
    command: python app.py
